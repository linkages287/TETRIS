# Requirements for Terminal Tetris
# Note: curses is typically included with Python on Unix-like systems
# If you're on Windows, you may need to install windows-curses:
# pip install windows-curses

# Python dependencies for weight visualization
numpy>=1.19.0
matplotlib>=3.3.0

# For Windows users, uncomment the line below:
# windows-curses
